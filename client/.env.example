# Leave empty for local dev (Vite proxies /api to the backend).
# If your API runs on another port, e.g. 8800:
# VITE_DEV_PROXY_TARGET=http://localhost:8800
#
# For production builds, set to your API origin (no trailing slash):
# VITE_APP_BASE_URL=https://your-api.up.railway.app
